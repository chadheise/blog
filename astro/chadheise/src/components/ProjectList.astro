---
import { parseAndSortProjectPosts, formatDate } from '../lib/blog';

interface Props {
  limit?: number;
}

const { limit } = Astro.props as Props;
const allPosts = await parseAndSortProjectPosts();
const posts = limit ? allPosts.slice(0, limit) : allPosts;
const showViewAll = limit !== undefined && allPosts.length > limit;
---

<div class="project-list">
  {posts.map((post) => (
    <a href={post.url} class="project-card">
      <div class="project-card-content">
        <h3 class="project-title">{post.title}</h3>
        <p class="project-date">{formatDate(post.frontmatter.date)}</p>
        <p class="project-excerpt">{post.excerpt}</p>
      </div>
    </a>
  ))}
</div>
{showViewAll && (
  <a href="/projects" class="view-all-link">View all projects →</a>
)}

<style>
  .project-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .project-card {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    margin: 0;
    padding: 1rem;
    border-radius: 8px;
    background-color: #E5DDD4;
    text-decoration: none;
    color: inherit;
    transition: background-color 0.2s ease;
  }

  .project-card:hover {
    background-color: #DDD5CC;
  }

  .project-card-content {
    flex: 1;
  }

  .project-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: #62664F;
    margin: 0 0 0.25rem 0;
  }

  .project-date {
    font-size: 0.875rem;
    color: #5A738F;
    font-weight: 400;
    margin: 0 0 0.5rem 0;
  }

  .project-excerpt {
    font-size: 0.95rem;
    color: #3E3E3E;
    line-height: 1.6;
    margin: 0;
  }

  .view-all-link {
    display: inline-block;
    margin-top: 1rem;
    color: #3E8CE6;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .view-all-link:hover {
    color: #2A5FA8;
    text-decoration: underline;
  }

  @media (max-width: 600px) {
    .project-list {
      gap: 0.75rem;
    }

    .project-card {
      padding: 0.75rem;
    }

    .project-title {
      font-size: 1rem;
    }

    .project-date {
      font-size: 0.8rem;
    }

    .project-excerpt {
      font-size: 0.875rem;
    }
  }
</style>
